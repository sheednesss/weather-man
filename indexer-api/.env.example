# Base Sepolia RPC URL (Alchemy, Infura, or other provider)
# For local testing with Anvil: http://127.0.0.1:8545
BASE_RPC_URL=https://base-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# MarketFactory contract address on Base Sepolia
# Use actual deployed address when available
MARKET_FACTORY_ADDRESS=0x0000000000000000000000000000000000000000

# Block number to start indexing from (use deployment block for efficiency)
# For local Anvil testing: 0
START_BLOCK=0

# Database URL (leave empty for PGlite in development, use postgres:// for production)
# Example: postgres://user:password@localhost:5432/ponder
DATABASE_URL=

# Weather API Configuration
# Open-Meteo API is free and does not require an API key
# Weather data is cached for 15 minutes (900 seconds) to avoid rate limits
# Cache TTL can be modified in src/lib/weather.ts

# Testing Instructions:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. For local testing with Anvil:
#    - Start Anvil: cd ../contracts && anvil --fork-url $BASE_SEPOLIA_RPC
#    - Update BASE_RPC_URL=http://127.0.0.1:8545
#    - Set MARKET_FACTORY_ADDRESS to deployed factory address
# 3. Run indexer: npm run dev
# 4. GraphQL playground: http://localhost:42069
#
# API Endpoints:
# - GET /weather/:cityId - Weather for single city (0=NYC, 1=Chicago, 2=Miami, 3=Austin)
# - GET /weather - Weather for all cities
# - GET /markets-with-weather - Markets with embedded weather data
# - GET /cache-stats - Weather cache statistics
# - GET /health - Health check
#
# Session secret for SIWE authentication (must be 32+ characters)
# Generate a secure secret for production: openssl rand -base64 32
SESSION_SECRET=your-secure-session-secret-must-be-32-chars

# Required: Node.js >= 18.14 (Ponder requirement)
